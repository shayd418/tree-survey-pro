/**
 * GovTemplate.js
 * =================================================================
 * Ministry of Agriculture official Excel report template.
 * Stored as a Base64-encoded .xlsx file and decoded at runtime
 * using SheetJS (xlsx library) to generate compliant survey reports.
 *
 * In the full production version this file contains the complete
 * Base64-encoded government template (~480 KB). It is omitted here
 * for IP protection — replace with your own licensed template.
 *
 * Usage (production):
 *   const workbook = XLSX.read(atob(GOV_TEMPLATE_B64), { type: 'binary' });
 * =================================================================
 */

// Full Base64 template omitted from demo version
const GOV_TEMPLATE_B64 = '';
